# Third-party notices

The prebuilt `viewer.bundle.js` contains open-source components used only by the reusable 3D walkthrough:

- three.js — MIT License — see `licenses/THREE-LICENSE.txt`
- three-mesh-bvh — MIT License — see `licenses/THREE-MESH-BVH-LICENSE.txt`

The source package versions are pinned in `package.json` and `package-lock.json`.
